<nav class="navbar navbar-auth navbar-expand-lg navbar-dark bg-transparent fixed-top shadow"  role="navigation">
    <div class="container">
        <a class="navbar-brand pt-0 pb-0" href="{{ path('homepage') }}">
            <img src="{{ asset('build/images/logo.png') }}" class="transparent-75" alt="{{ 'title.brand'|trans({},'base') }}" height="40">
        </a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarTop" aria-controls="navbarTop" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarTop">
            <div>
                <ul class="navbar-nav">
                    <li class="nav-item active">
                        <a href="{{ path('homepage') }}" class="nav-link">
                            <i class="fa fa-home"></i> {{ 'menu.homepage'|trans({},'base') }}
                            <span class="sr-only">(current)</span>
                        </a>
                    </li>
                    <li class="nav-item">
                        <a href="{{ path('homepage') }}" class="nav-link">
                            <i class="fa fa-phone"></i> {{ 'menu.contacts'|trans({},'base') }}
                        </a>
                    </li>
                    <li class="nav-item">
                        <a href="{{ path('homepage') }}" class="nav-link">
                            <i class="fa fa-feed"></i> {{ 'menu.feedback'|trans({},'base') }}
                        </a>
                    </li>
                    <li>
                        <div class="mb-2 ml-2">
                            <form class="" autocomplete="off">
                                <div class="form-search-item">
                                    <input class="text-white"  type="text" id="search-nav" required>
                                    <label for="search-nav">{{ 'menu.search'|trans({},'base') }}</label>
                                </div>
                                <div class="form-search-item">
                                    <button type="submit" class="btn btn-sm btn-link"><i class="fa fa-search"></i></button>
                                </div>
                            </form>
                        </div>
                    </li>
                </ul>
             </div>

            <div class="ml-auto">
                <ul class="navbar-nav">
                {#
                        {% if app.user %}
                            <li class="dropdown">
                                <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false" id="user">
                                    <i class="fa fa-user" aria-hidden="true"></i>
                                    <span class="caret"></span>
                                    <span class="sr-only">{{ app.user.fullname }}</span>
                                </a>
                                <ul class="dropdown-menu user" role="menu" aria-labelledby="user">
                                    <li>
                                        <a href="{{ path('homepage') }}">
                                            <i class="fa fa-edit" aria-hidden="true"></i> {{ 'menu.user'|trans }}
                                        </a>
                                    </li>
                                    <li class="divider"></li>
                                    <li>
                                        <a href="{{ path('homepage') }}">
                                            <i class="fa fa-sign-out" aria-hidden="true"></i> {{ 'menu.logout'|trans }}
                                        </a>
                                    </li>
                                </ul>
                            </li>
                        {% endif %}
                #}
                    <li class="nav-item dropdown">
                        <a href="#" class="nav-link dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false" id="locales">
                            <i class="fa fa-globe" aria-hidden="true"></i>
                            <span class="caret"></span>
                            <span class="sr-only">{{ 'menu.choose_language'|trans({},'base') }}</span>
                        </a>
                        <div class="dropdown-menu dropdown-menu-auth" aria-labelledby="locales">
                            {% for locale in locales() %}
                                <a class="dropdown-item"  href="{{ path(app.request.get('_route', 'homepage'), app.request.get('_route_params', [])|merge({_locale: locale.code})) }}">{{ locale.name|capitalize }} <small>{{ locale.code[0:2] }}</small></a>
                            {% endfor %}
                        </div>
                    </li>
                </ul>
            </div>
        </div>
    </div>
</nav>
